<%= form_with(model: product_review, local: true) do |form| %>
  <% if product_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_review.errors.count, "error") %> prohibited this product_review from being saved:</h2>

      <ul>
      <% product_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :product_review_author %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :product_review_comment %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating, id: :product_review_rating %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :product_review_product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
